# Gradle and build
/.gradle/
/build/

# 프론트엔드 빌드 결과물 (Docker 또는 npm run build:backend 로 자동 생성)
/src/main/resources/static/

# IDE
/.idea/
*.iml

# Ignore all source trees under src/test (as requested)
/src/test/


/mds/*

# 환경변수 파일 (보안상 커밋 금지)
.env
.env.local
.env.*.local

# 단, .env.example은 팀 공유를 위해 커밋 허용
!.env.example

